{
  "name": "the-rugplay-files-cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Bun executes TS natively - skipping tsc'",
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
